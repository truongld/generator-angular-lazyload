language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-angular-lazyload' ]; then cd .. && eval "mv $currentfolder generator-angular-lazyload" && cd generator-angular-lazyload; fi

